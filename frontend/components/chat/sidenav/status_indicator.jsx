import React from 'react'

const StatusIndicator = (props) => (
  <span className="left_icon">
    <div className={`status_indicator status_indicator--active`}></div>
  </span>
)

export default StatusIndicator
