import { connect } from 'react-redux'

import NewChatgroupForm from './new_chatgroup_form'

const mapStateToProps = (state, ownProps) => {

  return {

  };
};

const mapDispatchToProps = (dispatch, ownProps) => {

  return {

  };
};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(NewChatgroupForm);
